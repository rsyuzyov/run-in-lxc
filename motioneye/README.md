# MotionEye в LXC

Установка и настройка MotionEye — веб-системы видеонаблюдения в LXC контейнере Proxmox VE.

## Описание

**MotionEye** — это веб-интерфейс для управления камерами видеонаблюдения на базе motion. Позволяет:

- Подключать IP-камеры (RTSP, MJPEG, ONVIF)
- Работать с USB-камерами (V4L2)
- Настраивать детекцию движения
- Записывать видео и снимки по расписанию или событиям
- Просматривать камеры через браузер
- Отправлять уведомления при обнаружении движения

## Требования

### Ресурсы LXC контейнера

| Ресурс | Минимум | Рекомендуется |
|--------|---------|---------------|
| CPU    | 1 vCPU  | 2+ vCPU       |
| RAM    | 1 GB    | 2 GB          |
| Диск   | 8 GB    | 20+ GB (с записями) |

> **Примечание:** Требования зависят от количества камер и разрешения. Каждая камера 1080p добавляет ~0.5-1 vCPU нагрузки при обработке.

### Настройки LXC

Стандартный непривилегированный контейнер.

Для **USB-камер** требуется проброс устройств (см. раздел «USB-камеры»).

## Установка

### Быстрая установка

```bash
cd /root/run-in-lxc/motioneye
sudo ./install.sh
```

### С параметрами

```bash
# Кастомный порт и часовой пояс
sudo ./install.sh --port 8080 --timezone Europe/Moscow

# С NFS-хранилищем для записей
sudo ./install.sh --nfs-mount 192.168.1.100:/recordings

# Полный пример
sudo ./install.sh \
  --port 8765 \
  --timezone Europe/Moscow \
  --nfs-mount 192.168.1.100:/camera-recordings \
  --nfs-mount-point /mnt/recordings
```

### Параметры скрипта

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--port PORT` | Порт веб-интерфейса | 8765 |
| `--data-dir DIR` | Каталог данных | /var/lib/motioneye |
| `--timezone TZ` | Часовой пояс | системный |
| `--nfs-mount SERVER:PATH` | NFS-шара для записей | - |
| `--nfs-mount-point DIR` | Точка монтирования NFS | /mnt/recordings |
| `--check` | Только проверка совместимости | - |
| `--skip-start` | Не запускать сервис | - |

## Первоначальная настройка

### 1. Вход в веб-интерфейс

После установки откройте в браузере:

```
http://<IP_контейнера>:8765
```

**Учётные данные по умолчанию:**
- Логин: `admin`
- Пароль: (пустой)

> ⚠️ **Важно:** Сразу установите пароль администратора в настройках!

### 2. Добавление камеры

1. Нажмите выпадающее меню в левом верхнем углу
2. Выберите «Add Camera...»
3. Выберите тип камеры:
   - **Network Camera** — IP-камера (RTSP/MJPEG)
   - **Local Camera** — USB-камера
   - **Simple MJPEG Camera** — простой MJPEG поток

### 3. Настройка IP-камеры

Для RTSP камер укажите URL вида:
```
rtsp://user:password@192.168.1.50:554/stream1
```

Для ONVIF камер MotionEye автоматически определит параметры.

## Интеграции

### NFS-хранилище

Для хранения записей на сетевом хранилище используйте опцию `--nfs-mount`:

```bash
sudo ./install.sh --nfs-mount 192.168.1.100:/camera-recordings
```

**Ручная настройка NFS:**

```bash
# Установка клиента
apt install nfs-common

# Создание точки монтирования
mkdir -p /mnt/recordings

# Монтирование
mount -t nfs 192.168.1.100:/recordings /mnt/recordings

# Автомонтирование (добавить в /etc/fstab)
echo "192.168.1.100:/recordings /mnt/recordings nfs defaults,_netdev 0 0" >> /etc/fstab
```

После монтирования укажите путь `/mnt/recordings` в настройках камеры → File Storage → Root Directory.

### SMB/CIFS-хранилище

```bash
# Установка клиента
apt install cifs-utils

# Монтирование
mount -t cifs //192.168.1.100/recordings /mnt/recordings \
  -o username=user,password=pass,uid=0,gid=0

# Автомонтирование
echo "//192.168.1.100/recordings /mnt/recordings cifs username=user,password=pass,_netdev 0 0" >> /etc/fstab
```

### USB-камеры

Для работы USB-камер в LXC требуется проброс устройств.

**На хосте Proxmox** добавьте в конфиг контейнера `/etc/pve/lxc/<ID>.conf`:

```
# Разрешение доступа к видеоустройствам
lxc.cgroup2.devices.allow: c 81:* rwm

# Проброс конкретной камеры
lxc.mount.entry: /dev/video0 dev/video0 none bind,optional,create=file

# Или всех камер
lxc.mount.entry: /dev/video0 dev/video0 none bind,optional,create=file
lxc.mount.entry: /dev/video1 dev/video1 none bind,optional,create=file
```

После изменения перезапустите контейнер:

```bash
pct restart <ID>
```

**Проверка в контейнере:**

```bash
# Список видеоустройств
v4l2-ctl --list-devices

# Информация о камере
v4l2-ctl -d /dev/video0 --all
```

### Уведомления

MotionEye поддерживает отправку уведомлений при обнаружении движения:

- **Email** — через SMTP сервер
- **Webhook** — HTTP-запрос к внешнему сервису
- **Run Command** — выполнение произвольной команды

Настройка в веб-интерфейсе: Camera Settings → Motion Notifications.

**Пример webhook для Telegram:**

```bash
# Создайте скрипт /usr/local/bin/telegram-notify.sh
#!/bin/bash
BOT_TOKEN="your_bot_token"
CHAT_ID="your_chat_id"
MESSAGE="🎥 Движение на камере: $1"

curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
  -d "chat_id=${CHAT_ID}" \
  -d "text=${MESSAGE}"
```

В настройках камеры → Motion Notifications → Run A Command:
```
/usr/local/bin/telegram-notify.sh "%camera_name"
```

## Управление

### Сервис

```bash
# Статус
systemctl status motioneye

# Перезапуск
systemctl restart motioneye

# Остановка
systemctl stop motioneye

# Логи
journalctl -u motioneye -f
```

### Обновление

```bash
# Обновление MotionEye
pipx upgrade motioneye

# Перезапуск сервиса
systemctl restart motioneye
```

### Резервное копирование

**Конфигурация камер:**
```bash
tar -czvf motioneye-backup.tar.gz /etc/motioneye/
```

**Восстановление:**
```bash
tar -xzvf motioneye-backup.tar.gz -C /
systemctl restart motioneye
```

## Конфигурационные файлы

| Файл | Описание |
|------|----------|
| `/etc/motioneye/motioneye.conf` | Главный конфиг MotionEye |
| `/etc/motioneye/camera-*.conf` | Конфиги камер |
| `/var/log/motioneye/` | Логи |
| `/var/lib/motioneye/` | Данные (если не NFS) |

## Оптимизация производительности

### Снижение нагрузки CPU

1. **Уменьшите разрешение** — 720p вместо 1080p
2. **Снизьте FPS** — 10-15 вместо 25-30
3. **Отключите текстовое наложение** — Text Overlay → OFF
4. **Используйте аппаратное декодирование** — если поддерживается

### Настройки motion detection

- **Frame Change Threshold** — порог изменения кадра (5-15%)
- **Auto Noise Detection** — автоопределение шума
- **Mask** — маска областей исключения

## Решение проблем

### MotionEye не запускается

```bash
# Проверка логов
journalctl -u motioneye -n 100

# Ручной запуск для отладки
motioneye -c /etc/motioneye/motioneye.conf
```

### Камера не отображается

1. Проверьте URL камеры: `ffprobe rtsp://...`
2. Проверьте сетевую доступность: `ping <IP камеры>`
3. Убедитесь в правильности логина/пароля

### USB-камера не видна

```bash
# Проверка устройств
ls -la /dev/video*

# Если пусто — проверьте проброс в LXC конфиге
# На хосте Proxmox:
ls -la /dev/video*
```

### Высокая нагрузка CPU

1. Уменьшите разрешение и FPS
2. Отключите Motion Detection для камер, где не нужно
3. Проверьте, не записывается ли видео постоянно

### NFS не монтируется

```bash
# Проверка доступности сервера
showmount -e 192.168.1.100

# Проверка монтирования
mount | grep nfs

# Ручное монтирование с отладкой
mount -v -t nfs 192.168.1.100:/path /mnt/recordings
```

## Полезные ссылки

- [MotionEye на GitHub](https://github.com/motioneye-project/motioneye)
- [Motion Documentation](https://motion-project.github.io/motion_config.html)
- [Proxmox LXC Documentation](https://pve.proxmox.com/wiki/Linux_Container)

